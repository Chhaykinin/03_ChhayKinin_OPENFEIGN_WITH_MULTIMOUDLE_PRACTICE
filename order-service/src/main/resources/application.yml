spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5432/practice_feign
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
server:
  port: 8083